---
name: in-out
description: セッションの開始と終了。in で前回の引継ぎを読んで作業ブランチを作り、out で引継ぎを書いてコミットまで終える。「in」「out」「はじめ」「終わり」「おわり」「セッション開始」「セッション終了」で起動する。
---

# in / out

引数が `in` か `out` かで分岐する。引数が無ければユーザーに聞く。

## in（開始）

1. `docs/handoff.md` を読む。**無い、または AGENTS.md に「未記入」が残っている**なら初期設定が
   済んでいないので `setup` を実行する（`setup` は、ルール自体を直す回ならすぐ終わる）。
2. ①〜③を2〜3行でユーザーに伝える。
3. main から作業ブランチを作る（既に作業中のブランチがあればそれを使う）。プロダクトが main 以外の
   ブランチにある場合は、そのブランチを土台にする。
4. ③（次回やること）を今回の作業案として出す。ユーザーが別のことを言えばそちらを優先する。

## out（終了）

1. `docs/handoff.md` を今回の内容で上書きする。前回分は消す。
2. 失敗があれば `docs/failures.md` に1件追記する。既存行は消さない。
3. 作業ブランチにコミットして push する。ここまでは毎回行う。
4. PR を出すかユーザーに聞く。出さないならここで終わり。ブランチは残す。
   - 出さない場合、`ci-green` の必須化は効かない（main へ直接入れる経路を止める仕組みが PR 経由でしか
     働かないため）。出さない選択が続くようならユーザーに一度伝える。
5. 出す場合は PR を作り、CI が緑になったらマージする。赤なら直す。マージ後はブランチを削除する。

引継ぎに書かないもの: 恒久的なルール（AGENTS.md に書く）、失敗の記録（`docs/failures.md`。handoff は上書きされて消える）、進捗の詳細（コミット履歴にある）。
