# AGENTS.md

「未記入」の欄が残っている間は、`setup` を実行する。該当が無い欄は「なし」と書く。

## コマンド

- 開発サーバ: `pnpm --filter web dev`（初回のみ `pnpm install`）
- テスト（全件 / 1件だけ）: `pnpm --filter web test` / `pnpm --filter web test -- tests/format.test.ts`
- lint / 型チェック: `pnpm --filter web lint` / `pnpm --filter web typecheck`
- 必須の環境変数（名前だけ。値は書かない）: なし（`apps/web/.env.example` 参照）
- 共通の文言・値の置き場所: `apps/web/lib/content.ts`

補足（CI が回すもの）: ビルドは `pnpm -r build`、依存監査は `pnpm audit --audit-level moderate`。

## 実装の進め方

- 機能ごとに、中身を書く前に入口から出口までを1回通す。途中の各段は空の返事でよい。通ってから中身を実装する。
- 規約: なし

## 結合を増やさない

判定は「これを変えたとき何ファイル触るか。そのとき、他のファイルの中身まで読む必要があるか」。1ファイルで済み、他は呼び出し方だけ知っていれば足りる状態を保つ。

1. 同じ値・同じ文言を2箇所以上に書かない。上の置き場所に1つ書いて参照する。
2. 同じ処理を呼ぶ入口は1つにする。
3. 別々の機能が、同じ変数・同じ画面部品を書き換えない。
4. 他の機能の内部を直接触らない。公開された呼び出し方だけを通す。

行数上限の lint が鳴ったら、行数で割らず、後から単独で直したくなる塊ごとに分ける。全体を組み立てるファイルには実装を書かず、部品を呼ぶだけにする。

## 完了の証明

- 証拠は外部事実のみ: CI の run URL / commit SHA / 公開 URL。自己申告は証拠にしない。
- 緑にするためにチェックを削除・無効化・緩和しない。落ちたら実装側を直す。
- 中身のないテストを置かない。
- 失敗したことは `docs/failures.md` に追記する。既存行は消さない。
